---
interface Props {
  hover?: boolean;
  padding?: 'none' | 'sm' | 'md' | 'lg';
  class?: string;
}

const { hover = true, padding = 'md', class: className } = Astro.props;

const paddings: Record<string, string> = {
  none: '',
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8',
};
---

<div
  class:list={[
    'bg-white border-brutal',
    hover && 'hover-bg-brand',
    paddings[padding],
    className,
  ]}
>
  <slot />
</div>
